# Verificações realizadas

- JavaScript validado com `node --check`.
- `portfolio.json` e `vercel.json` validados como JSON.
- Nenhum ID HTML duplicado.
- Nenhuma referência local estática ausente.
- CSS com blocos balanceados.
- Rotas principais retornando HTTP 200 em servidor local.
- 62 trabalhos audiovisuais válidos no fallback local.
- Nove projetos de programação cadastrados.
- Links com nova aba protegidos com `noopener noreferrer`.
- Dados da planilha inseridos com `textContent`/elementos DOM, sem HTML externo.
- Modal limpa o iframe ao fechar e aceita `Esc`.
- Player usa domínio `youtube-nocookie.com`.
- Layout definido para desktop, tablet e celular.

## Validação recomendada após publicar

- Confirmar o CORS da planilha Google no domínio definitivo.
- Validar todos os vídeos no endereço público.
- Executar Lighthouse em mobile e desktop.
- Testar o formulário em Windows, Android e iOS com aplicativo de e-mail configurado.
